{application, stream, [
    {description, "packet streammentation engine"},
    {vsn, "0.0.1"},
    {registered,[stream_server, stream_root_sup]},
    {applications, [kernel]},
    {mod, {stream_app,[]}}
]}.
